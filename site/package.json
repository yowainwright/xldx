{
  "name": "xldx-site",
  "private": true,
  "workspaces": [
    "app",
    "worker"
  ],
  "scripts": {
    "dev": "bun run --filter xldx-app dev",
    "dev:worker": "bun run --filter xldx-worker dev",
    "build": "bun run --filter xldx-app build",
    "deploy:worker": "bun run --filter xldx-worker deploy",
    "test": "bun test"
  }
}
